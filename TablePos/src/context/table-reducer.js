import { ADD_ITEMS, SELECT_TABLE } from "./constant";

const PosReducer = (state, action) => {
  switch (action.type) {
    case ADD_ITEMS:
      return {};
    case SELECT_TABLE:
      return {};
  }
};
export default PosReducer;
